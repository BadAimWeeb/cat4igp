[workspace]
members = ["client", "server", "shared", "libfec"]
resolver = "3"

[workspace.dependencies]
cat4igp-shared = { path = "shared" }
cat4igp-client = { path = "client" }
cat4igp-server = { path = "server" }
chrono = { version = "0.4.44", features = ["serde"] }
